CREATE TABLE public.egate_simulation
(
    id                               text      NOT NULL,
    status                           text      NOT NULL,
    content                          text      NOT NULL,
    created_at                       timestamp,
    PRIMARY KEY (id)
);
