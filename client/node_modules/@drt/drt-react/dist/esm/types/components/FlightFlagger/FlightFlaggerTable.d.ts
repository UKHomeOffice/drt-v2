import React from "react";
import { FlightArrival } from "./FlightArrival";
export interface IFlightCard {
    flight: FlightArrival;
}
export declare const getConfidenceAlert: (confidence: string | undefined) => string[];
export interface IFlightFlaggerTable {
    flights: FlightArrival[];
}
export declare const FlightFlaggerTable: ({ flights, ...others }: IFlightFlaggerTable) => React.JSX.Element;
