import React from "react";
import { IconNames } from './DynamicIcon';
export type MenuItem = {
    label: string;
    link: string;
    roles?: string[];
    icon?: IconNames | string;
};
export interface IHeader {
    userRoles: string[];
    adminMenuItems: MenuItem[];
    leftMenuItems?: MenuItem[];
    rightMenuItems?: MenuItem[];
    portMenuItems: MenuItem[];
    initialSelectedPortMenuItem: string;
    maxWidth: string;
    routingFunction: (route: string) => void;
    logoutLink: () => void;
}
declare const Header: ({ userRoles, adminMenuItems, rightMenuItems, leftMenuItems, portMenuItems, initialSelectedPortMenuItem, maxWidth, routingFunction, logoutLink }: IHeader) => React.JSX.Element;
export default Header;
