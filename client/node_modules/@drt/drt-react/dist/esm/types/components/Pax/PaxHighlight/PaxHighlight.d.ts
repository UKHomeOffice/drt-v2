import React from "react";
import { DatasourceStatus } from "../PaxUtils";
export interface IPaxHighlight {
    status: DatasourceStatus;
}
export declare const PaxHighlight: ({ status }: IPaxHighlight) => React.JSX.Element;
