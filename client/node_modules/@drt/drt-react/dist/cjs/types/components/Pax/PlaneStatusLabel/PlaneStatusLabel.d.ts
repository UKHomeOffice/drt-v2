import * as React from 'react';
export declare enum PlaneStatus {
    Delayed = "Delayed",
    Cancelled = "Cancelled",
    Diverted = "Diverted",
    OnChocks = "On chocks",
    OnChocksDelayed = "On chocks (delayed)"
}
export interface IPlaneStatusLabel {
    status: PlaneStatus;
}
export declare const PlaneStatusLabel: ({ status }: IPlaneStatusLabel) => React.JSX.Element;
