import React from 'react';
export interface ShiftForm {
    id: number;
    name: string;
    startTime: string;
    endTime: string;
    defaultStaffNumber: number;
}
export interface ShiftsFormProps {
    port: string;
    terminal: string;
    interval: number;
    shiftForms: ShiftForm[];
    confirmHandler: (shiftForms: ShiftForm[]) => void;
}
export declare const AddShiftForm: ({ port, terminal, interval, shiftForms, confirmHandler }: ShiftsFormProps) => React.JSX.Element;
