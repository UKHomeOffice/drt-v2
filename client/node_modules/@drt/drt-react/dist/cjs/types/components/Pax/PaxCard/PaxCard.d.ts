import * as React from 'react';
export declare enum PaxTimeRange {
    Next5Mins = "Next 5 Mins",
    Next5to10Mins = "Next 5 to 10 Mins",
    Next10to15Mins = "Next 10 to 15 Mins"
}
export type PortQueue = {
    queueName: string;
    queueCount: number;
};
export interface IPaxCard {
    queues: PortQueue[];
    timeRange: PaxTimeRange;
    startTime: Date;
    endTime: Date;
}
export declare const PaxCard: ({ queues, timeRange, startTime, endTime }: IPaxCard) => React.JSX.Element;
