import moment from 'moment';
import * as React from 'react';
export declare enum PaxSearchFormDay {
    Yesterday = "yesterday",
    Today = "today",
    Tomorrow = "tomorrow"
}
export declare enum PaxSearchFormTime {
    Now = "now",
    Day = "24hour"
}
export type PaxSearchFormState = {
    day?: PaxSearchFormDay;
    time?: PaxSearchFormTime;
    arrivalDate: moment.Moment;
    fromDate: moment.Moment;
    toDate: moment.Moment;
    timeMachine?: boolean;
};
export type IPaxSearchForm = PaxSearchFormState & {
    onChange: (values: PaxSearchFormState) => void;
};
export declare const PaxSearchForm: ({ day, time, arrivalDate, fromDate, toDate, timeMachine, onChange }: IPaxSearchForm) => React.JSX.Element;
