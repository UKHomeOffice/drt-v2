import React from "react";
import { DatasourceStatus } from "../PaxUtils";
export interface IPaxDatasource {
    status: DatasourceStatus;
}
export declare const PaxDatasource: ({ status }: IPaxDatasource) => React.JSX.Element;
