import React from "react";
export interface IHighlight {
    color: string;
    text: string;
    tooltipText: string;
}
export declare const Highlight: ({ color, text, tooltipText }: IHighlight) => React.JSX.Element;
